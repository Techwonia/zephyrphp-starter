<?php

/**
 * Web Routes
 *
 * Your custom routes go here. Most ZephyrPHP sites won't need this file —
 * pages and content are managed through the CMS admin panel, and auth,
 * setup, and error routes are owned by their respective modules.
 *
 * Only add routes here for endpoints that need custom PHP logic — e.g.
 * webhook receivers with unusual auth, parameterised URLs, form handlers.
 */
